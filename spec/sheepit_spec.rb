# Encoding: UTF-8

require_relative 'spec_helper'

describe Sheepit do
  it 'has a version number' do
    expect(Sheepit::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
